language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t marcelobarreto/docker-frontend -f Dockerfile.dev .

script:
  - docker run -e CI=true marcelobarreto/docker-frontend npm run test -- --coverage